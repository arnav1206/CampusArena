# Campus Arena — Environment Variables
# Copy this file to .env and fill in your credentials

# ── PostgreSQL (required) ────────────────────────────────────────────────────
# Use the connection URL from Neon, Supabase, Railway, Render, or your own
# PostgreSQL instance. Set the same value in your production host's secrets.
DATABASE_URL=postgresql://username:password@localhost:5432/campus_arena

# ── Email OTP (Gmail) ─────────────────────────────────────────────────────────
# Create an App Password at https://myaccount.google.com/apppasswords
# Make sure "Less secure app access" or 2FA + App Password is enabled
GMAIL_USER=your.email@gmail.com
GMAIL_PASS=your-app-password-here

# ── SMS OTP (Fast2SMS — free Indian SMS API) ──────────────────────────────────
# Get a free API key at https://fast2sms.com/
FAST2SMS_KEY=your-fast2sms-api-key-here

# ── Platform administrator ──────────────────────────────────────────────────
# This exact email receives the platform-admin role after it verifies a real OTP.
# Replace it with an inbox you control before deployment.
PLATFORM_ADMIN_EMAIL=arnavgoel1206@gmail.com
PLATFORM_ADMIN_NAME=Platform Admin

# Used to sign administrator sessions. Generate a long random value and keep it secret.
AUTH_SESSION_SECRET=replace-with-a-long-random-secret

# ── Public demo accounts ────────────────────────────────────────────────────
# Lets the five seeded demo accounts use their documented password without an
# OTP delivery provider. Set to false for a non-demo production deployment.
DEMO_CREDENTIALS_ENABLED=true

# ── Server ────────────────────────────────────────────────────────────────────
PORT=5000
NODE_ENV=development
